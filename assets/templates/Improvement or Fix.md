---
status: open
---

> Copy this file into your project's plans location, rename it to describe the change, and fill in below. Use this instead of "New Feature" when the work changes or corrects existing behavior rather than adding new capability. Same status lifecycle: `open` → `in_progress`/`blocked` → `done`/`cancelled` (+ `completed: <date>`, then archive).

### Problem

What's wrong with current behavior, and what's the impact?

### Current Behavior

What happens today.

### Desired Behavior

What should happen instead.

### Affected repos / files

Which repos/services are involved. Reference known file paths if you already know them.

### Root Cause (if known)

Skip if this is a design change rather than a bug.

### Acceptance Criteria

How do we know this is fixed/improved?
