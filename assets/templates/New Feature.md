---
status: open
---

> Copy this file into your project's plans location, rename it to the feature name, and fill in below. `status`: `open` → `in_progress`/`blocked` while it's being worked → `done`/`cancelled` when finished, at which point also add a `completed: <date>` field and move the file to an archive location.

### Goal

What are we trying to achieve, and why now?

### Desired Behavior

Describe the new capability from the outside — what should exist that doesn't today.

### Scope

- In scope:
- Out of scope:

### Affected repos / files

Which repos/services are involved. Reference known file paths if you already know them — don't guess if you don't.

### Open Questions

Anything ambiguous that needs a decision before/during implementation.

### Acceptance Criteria

How do we know this is done?
